
//sets up image carousel using the slick library
$('.image-carousel-container').slick({
    dots: false,
    infinite: true,
    speed: 300,
    slidesToShow: 4,
    variableWidth: true,
    autoplay: true,
  autoplaySpeed: 2000,
  });